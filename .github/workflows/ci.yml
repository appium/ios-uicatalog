name: CI
on: pull_request

jobs:
  build:
    uses: ./.github/workflows/build-app.yml
    with:
      upload_release_artifacts: false
      upload_pr_artifacts: ${{ github.event_name == 'pull_request' }}
      run_lint: true
